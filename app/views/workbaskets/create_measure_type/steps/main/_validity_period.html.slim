fieldset
  h3.heading-medium
    | When is the measure type valid from?

  form-group :errors="errors" error-key="validity_start_date"
    template slot-scope="slotProps"
      label.form-label
        span.error-message v-if="slotProps.hasError" v-cloak=""
          | {{slotProps.error}}
        span.form-hint

      .bootstrap-row
        .col-md-6
          = content_tag "date-select", "", ":value.sync" => "measure_type.validity_start_date"

fieldset
  h3.heading-medium
    | When is the measure type valid until?

  form-group :errors="errors" error-key="validity_end_date"
    template slot-scope="slotProps"
      label.form-label
        span.error-message v-if="slotProps.hasError" v-cloak=""
          | {{slotProps.error}}
        span.form-hint
          | This is optional and should usually be left unset (open-ended) unless you know the measure type is only needed for a limited time.

      .bootstrap-row
        .col-md-6
          = content_tag "date-select", "", ":value.sync" => "measure_type.validity_end_date"

